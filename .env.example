# Neural Studio X — Environment Configuration
# Copy this to .env and fill in your values.
# NEVER commit .env to version control.

# Authentication cookie secret (change this in production!)
COOKIE_SECRET=nsx-super-secret-key-change-in-prod

# App environment: development | production
APP_ENV=development

# WakaTime API Key (optional)
WAKATIME_API_KEY=
